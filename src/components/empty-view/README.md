# EmptyView

EmptyView is used as a placeholder in `<Chart />` component.

## Props

It has none.

## Example

We don't have an example of `<EmptyView />`, it's used within the `<Chart />`. But you have the ability to render it anywhere.
